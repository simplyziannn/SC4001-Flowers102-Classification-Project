# -------------------------------------------------------------------------
# This requirements.txt is provided *only as a reference* to list
# all dependencies used in this project.
#
# Do NOT run:
#     pip install -r requirements.txt
#
# Some packages (especially PyTorch CUDA wheels) cannot be resolved
# through the default PyPI index and must be installed manually.
# Please follow the manual installation steps in the README instead.
# -------------------------------------------------------------------------

# Example wheels (choose the correct one for your system):
#   CUDA 12.6: pip install torch==2.9.0+cu126 torchvision==0.24.0+cu126 torchaudio==2.9.0+cu126 --index-url https://download.pytorch.org/whl/cu126
#   CUDA 12.4: pip install torch==2.9.0+cu124 ...
#   CUDA 12.1: pip install torch==2.9.0+cu121 ...
#   CUDA 11.8: pip install torch==2.9.0+cu118 ...
#

# Placeholder versions for reference only
torch==2.9.0
torchvision==0.24.0
torchaudio==2.9.0

# After installing PyTorch manually, install the rest of the dependencies
matplotlib
numpy
optuna
pandas
Pillow
scikit-learn
scipy
tqdm
joblib
networkx
